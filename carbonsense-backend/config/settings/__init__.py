"""
Settings package.

Pick a concrete settings module via DJANGO_SETTINGS_MODULE, e.g.:
    DJANGO_SETTINGS_MODULE=config.settings.dev   (default in manage.py / wsgi)
    DJANGO_SETTINGS_MODULE=config.settings.prod  (gunicorn / production)

Importing the package directly does nothing; always target a submodule.
"""
